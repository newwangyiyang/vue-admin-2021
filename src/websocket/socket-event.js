/**
 * 前后端约定触发socket的事件名
 * $emit去触发事件
 * $on监听该事件，让web页对该页面进行相应
 */
export const msg = 'msg';
export const chatList = 'chatList';
export const timestamp = 'timestamp';

// 实时报警
export const realTimeAlarmEvent = 'realTimeAlarmEvent';
